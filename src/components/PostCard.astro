---
import type { PostRecord, LegacyPost } from "../lib/types";

const { post } = Astro.props as { post: PostRecord | LegacyPost & { $id?: string } };
---

<article class="card">
  {post.coverImage ? <img src={post.coverImage} alt={post.title} /> : null}
  <h3><a href={`/library/${post.category}/${post.slug}/`}>{post.title}</a></h3>
  <p class="meta">
    {post.categoryLabel}
    {post.publishedAt ? ` · ${new Date(post.publishedAt).toLocaleDateString("en-US")}` : ""}
  </p>
  <p>{post.excerpt}</p>
  <p><a href={`/library/${post.category}/${post.slug}/`}>Read More &raquo;</a></p>
</article>
